# GitHub連携運用規程改定案 R-01是正版（レビュー用）

作業ID: `20260904-06`  
状態: **レビュー用是正版（未承認・未発効）**  
是正対象: `成果物/20260904-04_GitHub連携運用規程改定案.md` の監査指摘 R-01

## 1. R-01の原因と是正内容

独立監査報告（作業ID `20260904-05`）は、新規 `GitHub連携運用.md` 案が「承認済み個別作業指示又はNoriの明示指示」でGitHub操作を許容する一方、`AGENTS.md` と `AI間通信運用.md` の変更案は個別作業指示を必須と読めるため、書込み操作の承認経路が一意でないと指摘した。

本是正版は、次の三層をすべての変更案で同一に定義する。

| 操作 | 実施根拠 | 境界 |
| --- | --- | --- |
| GitHub上の情報の読取り・確認 | Noriの明示的な直接指示、承認済み `GitHub連携運用.md` の定常的参照運用、又は承認済み個別作業指示 | 読取りだけではローカル変更、Git設定変更、外部書込み、正式承認又は運用開始を意味しない。機密情報等は取得・表示・保存しない。 |
| GitHubへの新規作成・更新・commit・push等の書込み | 対象、範囲、保存先、公開条件、禁止事項、完了条件を明記した**承認済み個別作業指示** | `GitHub連携運用.md` の存在又はNoriの直接指示だけでは、任意の書込み権限を常設しない。 |
| 高リスク操作 | 承認済み個別作業指示 **に加え** Noriの明示承認 | remote又は認証変更、force push、履歴改変、公開範囲変更、新規リポジトリ作成等。 |

本是正はR-01に必要な承認経路の明確化だけを行う。正本・ミラーの関係、Public安全条件、ROLE、フォルダ構成、監査境界、保全対象及び禁止事項は原案から変更しない。

## 2. 新規 `GitHub連携運用.md` 完全案（是正版）

以下は、Nori承認後の正式保存先候補 `規程/WORKFLOW/GitHub連携運用.md` の本文案である。

---

# GitHub連携運用

## 目的

ファンドチームにおけるGitHub利用を、作業指示、成果物、差分、レビュー、受け渡し及び履歴管理の共有・参照用ミラー層として、安全かつ追跡可能に扱う。GitHubはローカルWorkspaceの規程、実装又は重要資産の正本を置き換えない。

## 適用範囲

本運用は、Noriが承認した `1 ChatGPTプロジェクト = 1 GitHubリポジトリ = 1 Codex Workspace` の対応に限り適用する。本運用は自動同期、Webhook、GitHub Actions、Codex自動起動、会話DB、認証情報管理又はPublic/Private方針の決定を導入しない。

## 正本と参照用ミラー

1. ローカルWorkspaceの承認済み規程、実装及び重要資産を正本とする。
2. GitHub上の `AGENTS.md`、`START_HERE.md`、`規程/` は、ChatGPT等が必要な現行規程を参照するための参照用ミラーであり、正本ではない。
3. GitHubからローカル規程を無条件に取得、上書き又は反映してはならない。
4. ローカル正本とミラーの内容、版、承認状態又は対象リポジトリに不一致がある場合、自動修正せず停止し、差分と影響をNoriへ報告する。

## 役割

| 主体 | 役割と制約 |
| --- | --- |
| Nori | 正本性、公開条件、個別GitHub操作、正式発行及び運用開始の最終判断者 |
| ChatGPT | 以下の承認経路に従い、GitHub上の参照用ミラー、作業指示又は公開可能なレビュー情報を参照又は作成する。GitHub内容だけでローカル作業の承認又は実施を確定しない。 |
| Codex | 正規WorkspaceとGitルートを確認し、以下の承認経路に従い成果物作成、差分確認、commit又はpushを実施する。既存ROLE及び個別指示を超えて権限を拡張しない。 |
| GitHub | 公開可能な情報の共有、履歴及び参照用ミラー層。正本、承認者又は自動実行基盤ではない。 |

## GitHub操作の承認経路

### 読取り・確認

GitHub上の承認済み文書、作業指示、成果物、差分又はcommit情報の読取り・確認は、次のいずれかを根拠として実施できる。

1. Noriの明示的な直接指示
2. 承認済み `GitHub連携運用.md` に定める定常的な参照運用
3. 承認済み個別作業指示

読取り・確認は、それ自体でローカルWorkspace変更、Git設定変更、外部書込み、正式承認、送達、受領、実行又は運用開始を意味しない。認証情報、個人情報、機密情報又は承認範囲外の外部情報を取得、表示又は保存してはならない。

### 書込み

GitHubへの新規作成、更新、commit、push、又は規程ミラー更新等の書込み操作は、対象、範囲、保存先、公開条件、禁止事項及び完了条件を明記した**Nori承認済み個別作業指示**に定められた範囲だけ実施できる。Noriの直接指示又は本運用の存在だけを根拠に、任意の書込み権限を常設しない。

書込み前に、正規Workspace、Gitルート、対象リポジトリ、remote、branch、既存未commit変更、stage対象、公開安全性及び個別指示との一致を確認する。既存変更を安全に分離できない場合は停止する。

### 高リスク操作

次の操作は、承認済み個別作業指示に含まれていても、別途Noriの明示承認を必要とする。

- remoteの追加、変更又は削除
- Git設定の恒久変更
- 認証方式、credential、access token、API key又はSSH keyの設定、表示又は共有
- force push、rebase等による既存共有履歴の実質的改変
- branch protection又はrepository rulesetの変更
- リポジトリのPublic / Private変更
- 新規リポジトリ作成又は削除
- 機密情報、個人情報又は重要資産の外部共有
- 規程、ROLE又は重要資産のGitHub正本化

## GitHubへ置ける情報と置けない情報

公開可能と明確に判断できる作業指示、成果物、レビュー、差分、commit記録及び承認済み規程の参照用ミラーだけを置ける。現行 `nori-sys/fund-team` はPublicであるため、公開可否に疑義がある情報は置かない。

次をGitHubへ置いてはならない。

- パスワード、APIキー、access token、認証コード、SSH秘密鍵その他の認証情報
- 個人情報、顧客・取引先情報その他の機密情報
- FChart原本、大容量株価データ、`database/fund_stock.db`、バックアップDB、ログ原本
- 公開可否、正本性又は保存権限が未確定の資産

## 作業指示・成果物の扱い

- `作業指示/` は、Nori承認済みの実行範囲を示す文書を共有・参照する用途に限る。GitHub上の表示又は取得だけで、Nori承認、送達、受領、実施開始を自動的に意味しない。
- `成果物/` は、承認済み作業の公開可能な成果物又は原報告を共有する用途に限る。未承認案は正式規程として扱わない。
- GitHub経由で受信した内容は、対象リポジトリ、branch、commit、対象ファイル、承認状態及び公開安全性を確認するまで実行根拠にしない。

## 基本フロー

### ChatGPT → GitHub → Codex

1. ChatGPTは、書込みについては承認済み個別作業指示の範囲でのみ、Nori承認済みかつ公開可能な作業指示を対象リポジトリ、保存先、branch及び公開条件とともに作成又は更新する。
2. Codexは、読取り・確認については本運用の承認経路に従い、正規Workspace、Gitルート、対象commit及び内容を確認する。
3. Codexは、ローカル正本と承認済み個別指示に矛盾しない場合だけ着手する。
4. 不一致、認証要求、公開安全性の疑義又は対象外変更を検知した場合は停止する。

### Codex → GitHub → ChatGPT

1. Codexは、書込みについては承認済み個別作業指示で許可された公開可能な成果物又は報告だけを対象にする。
2. commit前に、stage対象が承認済みのファイルだけであること、機密情報が含まれないこと、既存未commit変更が混入しないことを確認する。
3. 通常のcommit・push後、commit ID、branch、remote、送信ファイル及び検証結果を原報告する。
4. ChatGPTは対象commitを読取り・確認できても、承認、受領又は作業全体の完了を自動認定しない。

### 規程ミラー更新

1. ローカル正本の正式発行後、承認済み個別作業指示で指定された規程だけをミラー対象にする。
2. 更新前後に対象パス、内容、commit ID及び正本の承認根拠を確認する。
3. 不一致時は修正、pull、merge又は上書きを自動実行せず停止・報告する。

## AI間通信との関係

通常の逐次AI間通信の通信ID、Markdown正本及び状態管理は `AI間通信運用.md` に従う。本運用はGitHubを共有・履歴・参照用ミラー層として限定し、AI通信の正本、送達、受領又は承認を代替しない。

## Public / Private

Publicリポジトリでは、公開されても問題のない情報だけを扱う。Private化又はPublic許容範囲の標準方針は本運用では決定しない。公開可否が不明な場合は、Publicリポジトリに送信せずNoriへ判断を求める。

## 停止条件と障害時の復旧

正規Workspace、Gitルート、対象リポジトリ、remote、branch、承認状態又は公開安全性を確認できない場合は停止する。競合、認証要求、remote変更、履歴改変又は設定変更が必要な場合も停止する。

自動復旧、force push、reset、rebase又は認証設定変更は行わない。対象commit、remote応答、差分及び影響範囲を記録し、Noriの個別判断を待つ。ローカル正本又は保全対象の上書きによる解消を試みない。

## 監査と記録

正式規程への発行前は `監査運用.md` に従い、必要な独立監査、Noriレビュー及び明示承認を経る。承認済みGitHub操作は、対象、根拠、branch、commit ID、送信又は取得ファイル、検証結果及び未解決事項を原報告に残す。

---

## 3. `AGENTS.md` 最小変更案（是正版）

「ROLE・通信・記憶」の末尾に、次を追加する。

```markdown
GitHubは、Nori承認済みの `規程/WORKFLOW/GitHub連携運用.md` に定める範囲で、共有・履歴・参照用ミラー層として利用できる。GitHub上の情報の読取り・確認は、Noriの明示的な直接指示、承認済みGitHub連携運用の定常的参照運用又は承認済み個別作業指示を根拠として実施できる。GitHubへの新規作成、更新、commit、pushその他の書込みは、対象、範囲、保存先、公開条件、禁止事項及び完了条件を明記したNori承認済み個別作業指示に定められた範囲だけ実施できる。GitHub上の規程コピーはローカル正本を置き換えず、GitHubからの無条件な上書き、同期又は承認状態の推測を行わない。
```

次は、承認済み個別作業指示に加えてNoriの明示承認を必要とすることを、既存の個別承認対象として維持する。remote変更、恒久Git設定変更、認証方式変更、credential・access token・API key・SSH keyの設定・表示・共有、force push、履歴改変、branch protection又はrepository ruleset変更、公開範囲変更、新規リポジトリ作成又は削除、機密情報・個人情報・重要資産の外部共有及び規程・ROLE・重要資産のGitHub正本化。

## 4. `START_HERE.md` 最小変更案（是正版）

「プロジェクト固有の定義」の `フォルダ構成` の次に、次の1行を追加する。

```markdown
- GitHub連携: `規程/WORKFLOW/GitHub連携運用.md`（Nori承認済みの場合だけ参照）
```

「文書の位置付け」の末尾に、次を追加する。

```markdown
- GitHub上の `AGENTS.md`、`START_HERE.md` 及び `規程/` は参照用ミラーであり、ローカルWorkspaceの承認済み正式文書に優先しない。GitHub上の情報の読取り・確認は、Noriの明示的な直接指示、承認済み `GitHub連携運用.md` の定常的参照運用又は承認済み個別作業指示に従う。GitHubへの書込みは、対象、範囲、保存先、公開条件、禁止事項及び完了条件を明記したNori承認済み個別作業指示に定められた範囲だけ実施する。
```

開始順序、正規場所停止ゲート、承認済み作業指示の優先順位及び保全対象は変更しない。

## 5. `AI間通信運用.md` 最小変更案（是正版）

「目的と範囲」の末尾に、次を追加する。

```markdown
ただし、Nori承認済みの `GitHub連携運用.md` に定める共有・履歴・参照用ミラー層は、本運用とは区別して扱う。GitHub上の情報の読取り・確認は、Noriの明示的な直接指示、承認済みGitHub連携運用の定常的参照運用又は承認済み個別作業指示を根拠として実施できる。GitHubへの新規作成、更新、commit、pushその他の書込みは、対象、範囲、保存先、公開条件、禁止事項及び完了条件を明記したNori承認済み個別作業指示に定められた範囲だけ実施できる。この区別は、AI間通信のMarkdown正本、通信ID、状態管理、送達、受領又は承認の代替を意味しない。
```

## 6. R-01以外を変更していないことの確認

原案から維持した事項は、ローカル正本とGitHub参照用ミラーの分離、Publicリポジトリでの情報制限、認証情報・個人情報・機密情報・DB・FChart原本等の除外、未承認案を `規程/` に保存しないこと、通常AI間通信の正本・状態管理の維持、`フォルダ構成運用.md` とROLE文書の変更不要判断、及び正式発行前の独立監査とNori明示承認である。

本是正版で追加又は変更したのは、R-01の解消に必要な読取り、書込み及び高リスク操作の承認経路の明示だけである。

## 7. 未解決事項

1. Publicリポジトリを継続利用する範囲及び、Privateを必要とする将来案件の判断基準。
2. 規程ミラーの更新担当、更新頻度及び正本との一致確認方法の具体的な承認範囲。
3. GitHub上の作業指示を、どの証跡で「Nori承認済み」と識別するか。
4. 正式発行時の独立監査担当指定及びNoriレビューの順序。

## 8. 正式反映候補一覧

| 対象 | 反映内容 |
| --- | --- |
| `規程/WORKFLOW/GitHub連携運用.md` | 新規正式WORKFLOW（是正版全文） |
| `AGENTS.md` | 読取り・書込み・高リスク操作の承認経路を明記した限定許可経路 |
| `START_HERE.md` | GitHub連携の参照先、ミラーの非正本性及び操作経路を追加 |
| `規程/WORKFLOW/AI間通信運用.md` | GitHub共有層との分離と同一の承認経路を最小追記 |

`フォルダ構成運用.md`、ROLE文書、GitHub設定、remote、DB及び既存実装コードは、正式反映候補に含めない。

## 9. 主担当の自己検証

- 読取りは3根拠、書込みは承認済み個別作業指示だけ、高リスク操作は個別指示に加えNori明示承認、という同一の経路を全変更案へ記載した。
- Noriの直接指示だけでGitHub書込みが可能と読める表現を残していない。
- `GitHub連携運用.md` の存在だけで常設の書込み権限が生じない。
- 高リスクGit操作の別承認を維持した。
- 現行 `AGENTS.md` の資産保全・停止条件、及び `AI間通信運用.md` の通常通信権限を拡張していない。
- ローカル正本とGitHub参照用ミラーの関係を維持した。
- 未承認案を `規程/` に保存せず、正式規程、ROLE、GitHub設定及び保全対象を変更していない。
