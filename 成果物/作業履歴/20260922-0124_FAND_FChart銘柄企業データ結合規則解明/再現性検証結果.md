# 再現性検証結果

対応する正式作業指示書：`作業指示/20260922-0124_FAND_FChart銘柄企業データ結合規則解明_作業指示書.md`

## 検証方法

現行 `kabu.lst` と `Userkabu.ini` をCP932で行単位に読み取り、第4列の整数値を検査した。対象値を1始まりと見なし、3つの固定長企業ファイルに対して範囲・重複・レコード境界を検証した。原本への書込み、コピー、実行及び設定変更は行っていない。

## 結果

| 検証項目 | 成功 | 失敗・例外 | 判定 |
| --- | ---: | ---: | --- |
| `kabu.lst` と `Userkabu.ini` のコード・名称一致 | 4,289 | 0 | 確認済み |
| 第4列の非ゼロ値が企業レコード範囲内 | 4,240 | 0 | 確認済み |
| 非ゼロ値の一意性 | 4,240 | 0重複 | 確認済み |
| 3企業ファイルの同一座標計算 | 4,240 | 0 | 確認済み |
| 第4列0の例外 | 0 | 49 | 座標未確認 |
| `Zaimu.dat` の同一座標結合 | 0 | 構造未確定 | 採用不可 |

## 信頼度

- `Userkabu.ini` 第4列から企業3ファイル**候補座標**への変換：**有力**。全有効値が一意に収まり、3ファイルの固定長境界を再現する。企業同一性の意味付けは未確認である。
- 銘柄と企業の同一性保証：**未確定**。第4列の意味を明記する直接資料又は独立既知値による照合がない。
- 銘柄と `Zaimu.dat` の結合：**未確定**。固定長座標、キー、例外規則を確認できない。

## 完了条件の評価

全銘柄・企業・財務データを安全に一意結合する規則は、49件の未解決例外と `Zaimu.dat` の未確定構造により確立できなかった。FChartヘルプ・README、35件教師データ、旧版・現行版比較、実行ファイル内の対象名参照及び `Zaimu.dat` 構造を追加照合しても、これらを推測せず解消する直接証拠は得られなかったため、完了条件Bに該当する。

追加で価値がある証拠は、FChartのデータリンク画面又は公式仕様における第4列の項目名、49件の例外の意味、及び `Zaimu.dat` のキー定義である。

公開区分は個別に `公開可` と明記されていないため、本検証結果をPublic GitHubへ送信しない。
