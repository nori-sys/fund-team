# hold原因分類・追加証拠評価 v1

状態：固定入力から確認できる原因と追加手段の必要性を分けた0310検証結果。追加取得又は実装の承認ではない。

## 固定8セル

| 原因区分 | 該当セル | 既存資料で言えること | hold解除に必要な証拠 |
| --- | --- | --- | --- |
| 公式表示と比較系列・期間役割の対応 | 1～8 | context期間と書類単位の申告は観測済み。表示上の当期／比較期、会計基準・連結／単体の系列へ候補factを一意に結べない | 対象書類の公式表示・定義関係と候補の位置を結ぶ証跡 |
| 重複factの選別 | 3、4、7、8 | 同一キー・同値の重複がある。順序又は値一致では選べない | 公式書類内の表示位置・重複の役割を区別する証跡 |
| 訂正前後の採用出典 | 5～8 | 公式親関係と候補値一致を記録。採用すべき訂正後出典の意味は未確定 | 公式訂正書類と元書類の対応・採用関係の証跡 |
| 概念定義 | 1～8 | 対象2概念のQNameは保存済み2025年版公式taxonomyの定義・ラベルと一致 | この一致だけでfactの採用はできない。広いtaxonomy調査は不要 |
| value・unit・nil・符号 | 1～8 | 保存済み18参照の値・unitはセル内で一致、unitはJPY、nilは0件。符号を含む公式表示との意味照合は採用前に必要 | 公式表示との結び付きが成立した際に値・unit・符号を再照合 |

元の分類v3の機械可読`hold_reason`には会計基準、actual/forecast、期間役割、scope、taxonomy semanticsなど未確認項目が列挙される。これらは各factが誤りという認定ではない。今回の照合で公式taxonomyの概念定義一致は確認したが、factを表示系列・採用出典へ結ぶ欠落は残るため、元の理由列や元判定を書き換えない。

## 654候補全体の範囲

既存全体分類は`accepted=0 / hold=654 / rejected=0`で収支654件。固定8セルへの完全対応は18参照であり、追加次元による18参照はセル相対`rejected`に分けた。36参照は654件全体の別集計として加算しない。固定3社・7書類以外を調べず、654件全件の個別意味確定、理由別の全件件数、全`hold`解除はしていない。既知の全体的未採用理由は、表示・比較系列、重複、訂正後出典などの不足であるが、この8セルの理由を654件全件へ一律に割り当てない。

## 手段別の必要性

| 手段 | 今回の評価 | 判断条件 |
| --- | --- | --- |
| 保存済み追補・分類・公式taxonomy照合 | 実施済み。候補、概念、構造期間、元件数と未確定点を固定できた | 同じ版を再利用し、元成果物は読取専用で保持 |
| 保存済み書類・証跡だけの追加照合 | 公式表示とfactの対応を立証できる本文・linkbase等は既存の抽出証跡にない。既存記録の再読だけでは残る欠落を埋められない | 新たな同種の読取り反復を進捗としない |
| 追加公式資料又は本文取得 | 表示系列・重複・訂正関係の確認に必要となる可能性が高い。対象は固定8セルに直接関係する既存書類部分へ限定すべき | 取得対象、最小量、期待効果、利用・権利条件、隔離保存先を別指示で確定してからNori判断。0310では要求0件 |
| 新規検証コード・DB又はPoC再実行 | 現時点の不足は証拠の欠落であり、新コードだけでは解消しない。証拠取得後に機械的照合が必要なら別途設計 | 対象、隔離先、入力・出力版、非干渉・監査方法を別承認。0310では実装0件 |
| 対象企業・年度・指標・書類の拡大 | 固定8セルの未解決条件を解消する手段として不要 | 本指示では対象外 |

Phase 4の合成証跡で成立した`accepted`遷移は実入力の根拠には使わない。今回も実データ`accepted`は0件でよく、根拠のない`hold`解除は0件である。

公開安全性：実識別子、書類ID、抽出値、取得URL、非公開ハッシュ及び隔離証跡本文を記載しない。
