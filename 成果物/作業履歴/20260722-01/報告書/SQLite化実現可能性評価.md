# SQLite化実現可能性評価

作業ID: 20260722-01

## 判定

限定的なSQLite化は可能。ただし正式運用DBへの登録は不可。

## 進められる範囲

- 原本ファイル情報、ハッシュ、サイズ、更新日時の管理テーブル化。
- `kigyo.dat` / `kigyo2.dat` / `kigyo3.dat` のレコード番号単位のRAW取り込み。
- `kigyo.dat` の先頭4 byteコード候補、日付候補、企業概要候補の抽出テーブル化。
- `kabu.lst` のCP932 CSV風テキストを銘柄マスタ候補として取り込む試作。

## 進めてはいけない範囲

- 売上高、利益、EPS等の正式項目名を割り当てること。
- 未検証値を既存 `database/fund_stock.db` に追加すること。
- `Zaimu.dat` を正式仕様として固定すること。

## 次工程の条件

- `kabu.lst` と `kigyo.dat` の全件順序照合。
- 7203、6506、8267、2802、4755、9984など5銘柄以上のFChart画面値提供。
- 複数年度の売上高、営業利益、純利益、EPS、決算期、発表日等の照合値提供。
- `Zaimu.dat` のレコード境界追加調査。

## 推奨

次工程は「限定解析」とし、RAWテーブルと検証済み候補テーブルを分ける。正式財務項目テーブルは、画面値照合後に設計する。
