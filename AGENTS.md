# AGENTS.md

## 目的

この文書は、ファンドチームWorkspaceで作業するAIおよび担当者に共通する最上位の行動原則を定める。プロジェクト固有の参照先、資産保全および作業開始条件は、`START_HERE.md` と承認済みの作業指示に従う。

## オーナー

全プロジェクトのオーナーはNoriである。Noriは、目的、優先順位、許容リスクおよび完了基準を決定する。

## 基本姿勢

- 事実、推測、提案および未決事項を区別する。
- 現行規程、承認済み作業指示およびNoriの最新指示を優先する。
- 過去の報告書、作業履歴、会話、引き継ぎまたは参考資料を、現行の正式決定や実行根拠として扱わない。
- 目的、対象、権限、保存先または承認状態が不明確な場合は、操作を停止してNoriへ確認する。

## 起動時正規場所停止ゲート

他のFUNDプロジェクト文書を自発的に読む前に、現在地とGitルートが `E:\AI ワークスペース\CODEX\ファンドチーム_Workspace` と一致することを確認する。失敗・未実行・確認不能の場合は、この確認とNoriへの事実報告以外を行わない。成功後だけ、次の開始時確認を行う。ファイル操作の直前にも同じ確認を再実施し、不一致なら書込みを行わない。Git確認が失敗しても恒久設定を変更しない。

## 開始時の確認

1. `AGENTS.md`
2. `START_HERE.md`
3. 承認済みの作業指示
4. 作業に必要な既存資料
5. `handover.md` が存在する場合は、必要に応じて確認する

必須文書が存在するはずなのに読めない場合は、作業を開始せずNoriへ報告する。存在が未確定の文書は、推測で再作成しない。

## 正規作業場所

ファイルの作成、更新、移動、改名または削除の直前に、現在地とGitルートが `E:\AI ワークスペース\CODEX\ファンドチーム_Workspace` と一致することを確認する。

```powershell
Get-Location
git rev-parse --show-toplevel
```

現在地またはGitルートが正規作業場所と異なる場合、書込み操作を行わない。Codexなどが作成したworktreeは、Noriが明示しない限り正規作業場所と同一視しない。Git所有者の不一致などによりGit確認ができない場合、設定を恒久変更せず停止してNoriへ報告する。

## 権限と承認

次の事項は、Noriの明示承認なしに確定・実施しない。

- 最上位規則、ROLE、規程または標準手順の新設・更新・廃止
- 正本性、保存期間、変更権限または外部共有に関する決定
- 既存資産の移動、改名、削除、上書き、統合または不可逆な変更
- DB、バックアップ、ログ、設定、実装コードまたは外部FChart原本への操作
- AI間通信、記憶、会話DBまたは共有機能の導入・変更
- 外部影響、認証情報、個人情報、機密情報または重大なリスクを伴う操作
- プロジェクト全体の完了判断

承認済みの作業指示に対象、範囲、禁止事項および完了条件が明記されている場合は、その範囲で実施できる。文書案の作成、評価、送達または内容受理は、正式発行や実施の承認とは扱わない。

## 資産の保全

次の原本群は、別途承認まで保全対象とする。

- `database/fund_stock.db`
- `database/backup/`
- `logs/`
- `C:\fchart\` の原本群
- 設定ファイルおよび実装コード

これらの正本性、変更権限および保存期間は未決である。未決事項を、既存資料の記載だけで確定してはならない。既存の `作業指示/`、`成果物/`、`tools/`、`work/` および `.agents/` についても、承認範囲外の移動、改名、削除または内容変更を行わない。

## 文書と記録の扱い

- `README.md` は説明資料であり、入口文書または現行規程として扱わない。
- 承認済みの `作業指示/` 配下の文書だけを、具体的な実行範囲の根拠とする。
- 過去の成果物、報告書および作業履歴は、現行規程や承認済み作業指示を上書きしない。
- 既存ファイルを空の内容で上書きしない。
- ファイルを単純削除しない。削除が必要な場合は、Nori承認後に指定された方法で退避または処理する。
- 個人情報、認証情報および機密情報を外部送信、画面表示または成果物へ転記しない。

## ROLE・通信・記憶

現行ROLEは `規程/ROLE/ROLE_INDEX.md` で確認する。ROLE_INDEXは参照索引であり、新たな権限を付与しない。AI間通信は `規程/WORKFLOW/AI間通信運用.md`、スレッド引き継ぎは `規程/WORKFLOW/スレッド引き継ぎ運用.md`、永続記憶・引き継ぎは `規程/WORKFLOW/永続記憶・引き継ぎ運用.md`、監査は `規程/WORKFLOW/監査運用.md`、保存先は `規程/WORKFLOW/フォルダ構成運用.md` に従う。

未承認の提案、通信記録、会話ログ、記憶または監査結果は、実行根拠、正式決定、正式発行または作業全体の完了判断にしない。会話DBの実施、共有検索、同期、自動通知、並行通信、外部共有、DB変更は別途承認を必要とする。

GitHubは、Nori承認済みの `規程/WORKFLOW/GitHub連携運用.md` に定める範囲で、共有・履歴・参照用ミラー層として利用できる。GitHub上の情報の読取り・確認は、Noriの明示的な直接指示、承認済みGitHub連携運用の定常的参照運用又は承認済み個別作業指示を根拠として実施できる。GitHubへの新規作成、更新、commit、pushその他の書込みは、対象、範囲、保存先、公開条件、禁止事項及び完了条件を明記したNori承認済み個別作業指示に定められた範囲だけ実施できる。GitHub上の規程コピーはローカル正本を置き換えず、GitHubからの無条件な上書き、同期又は承認状態の推測を行わない。

remote変更、恒久Git設定変更、認証方式変更、credential・access token・API key・SSH keyの設定・表示・共有、force push、履歴改変、branch protection又はrepository ruleset変更、公開範囲変更、新規リポジトリ作成又は削除、機密情報・個人情報・重要資産の外部共有及び規程・ROLE・重要資産のGitHub正本化は、承認済み個別作業指示に加えてNoriの明示承認を必要とする。

## 停止条件

- 正規作業場所、承認状態、対象、根拠または保存先が不明確である。
- 現行規程、承認済み作業指示およびNoriの最新指示が矛盾する。
- 保護対象、データ完全性、機密性または外部影響にリスクがある。
- ROLE、通信相手または権限の根拠が不明確である。
- 実施中に承認範囲を超える操作が必要になった。
