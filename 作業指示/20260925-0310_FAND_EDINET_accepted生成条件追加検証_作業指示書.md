# FAND EDINET accepted生成条件追加検証 作業指示書

> 状態：正式発行（Nori正式承認済み）
> 公開区分：公開可（原則公開可・禁止情報のみ除外）。
> 承認対象：ChatGPT/作業指示案/20260925-0310_FAND_EDINET_accepted生成条件追加検証_監査是正_作業指示書案.md。01）以降の本文は監査適合版と同一。

## 01）目的と前提

1332「FAND EDINET企業業績データ基盤 Phase1-5統合推進」の版固定成果物を用い、固定中核8セルについて、EDINET公式情報から企業業績値を`accepted`と判定するための公式根拠、識別条件及び決定論的な判定規則を追加検証する。現行の実入力全体状態は654候補＝`accepted`0件・`hold`654件・`rejected`0件である。Phase 4の`hold`保持・検索・再評価・非混入・再実行は隔離PoCで成立したが、`accepted`への遷移は合成証跡による試験であり、実入力の一意採用を証明していない。

目的及び合格条件は`accepted`件数を増やすことではない。公式根拠と固定入力から一意採用できる場合だけ新しい評価案で`accepted`とし、根拠不足、競合又は訂正関係の不明が残れば`hold`を維持する。何が確認済みか、何が未確認か、追加証拠又は実装が本当に必要かを判断できる資料を作る。本案の監査適合も、1332実データの状態変更、本番採用又は本作業開始を意味しない。

## 02）対象・範囲

基礎資料は1332正式作業のPhase 1～4最終適合版、Phase 1～4統合報告、Phase 5本番採用判断資料・最終独立監査、654候補の集計、中核8セルの候補集合・`hold`理由、Phase 4隔離PoCのモデル・判定機構に限定する。これらは版・適合状態を照合したうえで読取専用で参照し、既存成果物、隔離入力、PoC DB・コード・マニフェスト・状態記録を更新しない。1332の取得権限・隔離書込み権限・実装権限を本案へ継承しない。

実データ検証は1332で固定した同じ2社、税引前利益と当期純利益、2024年3月期比較値と2025年3月期当期値の8セル及び既存のセル相対36候補参照（完全対応`hold`18件、追加次元によるセル相対`rejected`18件）を対象とする。固定3社・7書類の保存済み入力以外の企業、年度、指標、書類、セルへ拡大しない。654候補全件の元の全体状態は`hold`654件のまま保持する。全体については既存分類の状態・件数収支・既知の理由区分を確認する範囲とし、全件の個別意味確定や全`hold`解除を要求しない。

8セルの採用可否に実際に必要な範囲で、指標とconcept QName・taxonomy・企業拡張の対応、context/periodと当期・比較期、書類単位又は公式メタデータによる比較系列の会計基準・連結／単体、必要なdimension/member、重複fact、訂正前後、value・unit・nil・符号、provenance、`accepted`/`hold`/`rejected`の境界を確認する。全taxonomy、9軸、全企業拡張の網羅的な解明や、fact単位の過剰な会計基準・連結区分証明を行わない。

## 03）保存先と開始前ゲート

正式承認後に本作業固有の公開安全化済み報告、仕様案、集計及び監査結果を置く候補先は、正規Workspace内の`成果物/作業履歴/20260925-0310_FAND_EDINET_accepted生成条件追加検証/`とする。正式作業指示書を作業履歴へ複製しない。今回の案監査では当該作業履歴を作成しない。

実識別子、書類ID、抽出値、取得URL、候補単位の比較、非公開ハッシュ等を含む新規検証記録の候補先は、正規Workspace `E:\AI ワークスペース\CODEX\ファンドチーム_Workspace` の既存1332隔離領域の**新規専用子ディレクトリ** `作業中\20260923-1332_EDINET_Phase1-5\20260925-0310_accepted条件追加検証`、絶対パス `E:\AI ワークスペース\CODEX\ファンドチーム_Workspace\作業中\20260923-1332_EDINET_Phase1-5\20260925-0310_accepted条件追加検証` とする。1332既存ファイルとは分離し、上書き・移動・削除しない。この保存先の提案は作成・使用の承認ではない。

正式発行時にNoriが上記絶対パス、読取対象、保存情報及びアクセス境界を明示承認した場合に限り、最初の非公開実データ書込み前に次を照合する。正規作業場所とGitルートを確認し、確認済みルートから相対構成要素を結合して実体・再解析ポイントを確認する。許可主体はNoriのWindowsアカウント、当該アカウントで動く作業担当、`SYSTEM`及び`Administrators`に限定し、許可権限は前二者に必要な読書き、後二者に管理・復旧に必要な権限とする。親からの継承を含む実ACLで広い主体、不明な主体又は想定外の権限があれば書き込まず停止する。不明SIDの既知監視状態は1332の`Monitor / Hold`と`Hard Stop`を区別し、削除又はACL変更を黙示実施しない。該当子ディレクトリが既存の`/作業中/20260923-1332_EDINET_Phase1-5/` Git除外内にあり、対象ファイルが追跡・stageされていないことを確認する。Git除外・ACL変更が必要なら実行せずNori判断を求める。照合日時、承認根拠、場所、ACL、Git状態及び未決事項は公開区分に従い記録し、公開記録へ実値等を載せない。いずれか確認不能なら非公開記録の作成・移動・取得を停止する。

## 04）制約・禁止事項

### A. 判定と既存資産

- AIの意味解釈、名称の類似又は推測だけで`accepted`を作らない。採用候補は公式根拠、fact識別、期間役割、値・unit、出典、訂正関係と同一入力・同一処理版での再現を満たす場合に限る。欠ける条件は明示して`hold`を維持する。
- `accepted`、`hold`、`rejected`を区別する。採用可能なのは、公式根拠に基づく全体判定と対象セル相対判定の両方が`accepted`で、当該セル内で一意な候補だけとする。`hold`及びセル相対`rejected`を採用値、採用検索又は既存のaccepted集計へ混入させない。セル相対`rejected`をfact自体の無効認定へ読み替えない。
- 1332の既存入力、654候補の全体状態、成果物、PoCコード・DB及びマニフェストは読取専用とし、新たな評価は版付きの別記録として提示する。合成証跡の成功を実データの`accepted`成立と記述しない。
- 本番DB、正式コード・設定・API設定、本番運用資産、FChart原本、既存バックアップ・ログには読取り・参照・接続・変更・書込みを行わない。ただし、承認範囲内の非干渉確認に必要なパス・状態等のメタデータ確認は除く。秘密情報を探索、表示、保存又は再利用しない。原本ZIP/XBRL/XML、PDF、一時RAWを新規ファイル化しない。既存資産を単純削除しない。

### B. 追加取得・再実装の境界

保存済み入力、証跡、公式資料だけを再利用する。新規の企業書類本文/API要求、外部資料、taxonomy版、対象企業・年度・指標・書類の取得・追加を本指示では行わない。継続取得、監視、網羅調査も行わない。必要性が判明した場合は対象、理由、最小量、期待効果、公開・権利条件を次工程案に記し、実行前に停止してNori判断を求める。1332の未使用・失敗した取得枠を再利用しない。

新しい検証コード・DB、既存PoCの修正・再実行を本指示では行わない。既存成果物の読取と、必要な決定論的な照合・表計算の範囲で評価する。新規実装が必要と判明した場合は必要性、対象、隔離先、検証方法を提案にまとめて停止する。大量のXBRL・XML・JSON等をAIへ全文投入せず、既存の集計・抽出証跡を先に用いる。

## 05）成果物

1. `accepted`判定条件仕様案：必須根拠、識別・採用・`hold`・`rejected`条件を、確認済みと未確認に分けて記載する。
2. 中核8セル再評価結果：各セルの既存候補、適用規則、根拠、今回の評価案、未確定理由を示し、1332の元状態と区別する。実値を含む版は指定隔離先だけに置く。
3. `hold`原因分類表：8セルの未採用理由と、654候補全体について既存証跡から言える集計・既知の理由・限界を区別する。
4. 追加証拠・実装の必要性評価：既存資料で足りるもの、追加公式資料、新規取得、実装変更を区分し、未承認の実行には進まない。
5. 次工程判断資料、公開安全化済み作業報告書、カンによる独立監査結果。

## 06）合格条件・完了条件

次をすべて満たす。実データ`accepted`が0件のままでも完了可能である。

1. 8セルの判定に必要な条件、公式根拠へ追跡できた範囲及び未確認の条件を区分し、根拠不足を断定で埋めていない。
2. `accepted`/`hold`/`rejected`の境界と入力版・処理版を明示し、同じ根拠から同じ今回の評価案を再現できる。既存1332の状態は変更していない。
3. 固定8セルすべてに規則を適用し、候補と理由を説明できる。654件全件の個別意味確定は要しない。
4. 実データ候補を`accepted`とする場合に限り、公式根拠による全体判定・対象セル相対判定の両`accepted`、セル内一意性、出典、当期／比較期、値・unit、訂正前後及び再現の証拠を第三者が確認できる。満たさなければ`hold`とし、セル相対`rejected`候補を採用しない。
5. `accepted`が0件のままなら、8セルそれぞれの`hold`を解除できない理由・不足根拠及びその確認限界を記載する。654候補については元の全体状態・件数収支と既知の集計を保持する。
6. 不足根拠を既存資料で解消可能か、追加公式資料・新規取得・実装が必要かに分類し、未承認の手段を実行していない。
7. 根拠のない`hold`解除、採用値への`hold`混入、合成証跡を実入力とする誤認が0件である。
8. 本番資産・既存1332成果物に変更がなく、非公開情報の保存・公開境界を満たす。
9. 未解決事項、次工程候補及びNori判断が要る事項を整理している。
10. 独立したカンの監査が「適合（承認可）」である。

成立した場合でも本番採用へ自動移行しない。実データで成立なら本番候補方式としてNori判断資料へ、公式証拠が不足なら取得対象を限定した別承認案へ、安全な生成が困難なら理由と代替方式候補へ分岐する。どの分岐も実行権限を新設しない。

## 07）公開区分

公開可（原則公開可・禁止情報のみ除外）。ただし、実在企業・書類の識別子、取得URL、実抽出値、再現可能な実データ、非公開マニフェスト・ハッシュ、隔離DB、認証情報、第三者の権利・契約上公開できない情報は公開しない。成果物単位でGitHub連携運用の公開安全性を照合し、除外後の資料も禁止情報を実質的に再現する、又は意味・証拠性が損なわれる場合は公開しない。公開判断が不明なら送信せずNori判断を求める。

## 08）停止条件と承認境界

正式承認・正式発行前は本案の実作業を開始しない。承認範囲内の技術的問題と監査指摘は、担当者が是正し、起案・是正に関与しないカンが独立再監査する。目的変更、対象・期間・指標・書類拡大、新たな取得・実装・権限・隔離先変更、本番資産操作、規程との重大な矛盾、保存先・ACL・Git状態の未確認又はNoriの方針判断が必要なら停止してNoriへ上げる。監査適合は正式発行、実作業開始、本番採用の承認を代替しない。
