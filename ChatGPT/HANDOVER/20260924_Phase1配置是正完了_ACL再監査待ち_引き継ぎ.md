# SOSIA FAND 司令 引き継ぎ

作成日：2026-09-24 JST

## 現在位置

1332正式作業はPhase 1進行中。固定対象7書類の本文取得は完走済み。

独立監査で実データ61ファイルの保存先誤りが判明したが、配置是正は完了した。

正規Workspaceは1フォルダ名の `ファンドチーム_Workspace`。誤配置側は `ファンドチーム` 配下の `_Workspace` という2階層。

61ファイルは正規Workspace配下の `作業中/20260923-1332_EDINET_Phase1-5` へ移動済み。移動前後で相対ファイル名、件数、サイズ、SHA-256が全件一致。誤配置側の対象61ファイルは0件。上書き、単純削除、Git変更操作、外部公開、ACL変更は行っていない。

正規側に元から存在した `.pyc` 8件は件数・サイズ・SHA-256・ACLとも不変。

## 隔離ゲート

Phase 1隔離ゲートは未解除。

指定先フォルダではACL継承が有効で、`Authenticated Users` に変更権限、`Users` に読取り権限が存在する。ACL是正はまだ未実施。

以前確認された不明SIDはMonitor / Hold事項として継続。

## 次工程

`ACL是正 → 隔離状態確認 → 独立再監査 → 必要な是正 → 再監査 → 適合`

Phase 1が適合するまでPhase 2へ進まない。

重要パスは、`ファンドチーム_Workspace`（1フォルダ）と `ファンドチーム/_Workspace`（2階層）を厳密に区別する。必要に応じて構成要素または文字コードで生成・検証してから操作する。
