# SOSIA FAND 司令 引き継ぎ

## 今回の到達点

Data Gateway最小レファレンス実装完了後、次段階の `ChatGPT → GitHub → Data Gateway` 設計へ進んだ。

その過程で、NoriがChatGPTとCodex間の中継役となることが作業ボトルネックになる問題を整理し、今後のCodex向け作業指示書は、Nori判断が必要な事項を除き、Codex側だけで目標達成まで進める方針とした。

特に監査工程は、承認済み作業指示の範囲内で `こう → カン → こう修正 → カン再監査` を `適合（承認可）` まで反復する設計へ改訂する方向で合意した。

## 新たに確定した運用方針

- 今後、すべてのプロジェクトのCodex向け指示書は、目標・対象・制約・成果物・完了条件・停止条件を明確化し、Nori判断が不要な作業はCodex側で自律完結させる。
- Noriへ戻すのは、承認範囲変更、正式規程・ROLE・権限変更、重要資産への重大変更、外部公開、機密・認証情報、重大な安全問題、解消不能な矛盾など、人間判断が必要な場合に限定する。
- 監査担当カンは独立性を維持し、起案・実装・是正を行わない。
- `適合（承認可）` はNoriの最終承認を代替しない。

## ChatGPT作業指示案フォルダ

ChatGPTが作成する未承認のCodex向け作業指示案を置く専用領域として、次を採用する方針をNoriが承認した。

`ChatGPT/作業指示案/`

ローカルWorkspaceにもコウが次を作成済み。

`E:\AI ワークスペース\CODEX\ファンドチーム_Workspace\ChatGPT\作業指示案`

用途を示す `README.md` も作成され、GitHub `main` への反映が完了している。

GitHub最終commit:

`bf82548876746660a67b05f32408ef8dca139468`

この領域の文書は、未承認・未発行であり、Codexの実行根拠ではない。Nori正式承認後のみ正式な `作業指示/` 運用へ移行する。

## GitHub同期問題の発見

ChatGPTがGitHub `main` を直接更新する一方、Codexローカル `main` が追随していなかったため、Codexからのpushがnon-fast-forwardで拒否された。

安全な対応として、正規Workspaceの既存未コミット変更には触れず、`origin/main` 起点の一時worktreeでREADME commitのみをcherry-pickし、fast-forward通常pushで反映した。

その結果、以下を新たな標準候補とした。

- CodexはGitHub書込み前に必ずremote最新状態を取得・確認する。
- ローカルbranchとremote branchの先行・遅延・分岐を確認する。
- 未コミット／未追跡変更がある場合、安易にpull・merge・rebase・reset・stash等を行わない。
- 必要に応じて一時branch／worktree等で隔離して処理する。
- `ローカルmain = GitHub main` を常に仮定しない。

現時点で正規Workspaceの `main` は既存変更保全のためGitHub `main` と未同期。報告時点でローカル先行1・遅延26。これは意図して残している。

## 現在作成済みの規程改訂指示書案

最新案:

`20260916-1820_SOSIA_FAND_自律運用_GitHub連携規程改訂_作業指示書案.md`

内容は次をまとめて改訂するもの。

- `規程/ROLE/ROLE_FUND_こう_ChiefManager.md`
- `規程/WORKFLOW/監査運用.md`
- `規程/WORKFLOW/GitHub連携運用.md`
- `規程/WORKFLOW/フォルダ構成運用.md`

主な改訂目的:

1. コウ⇄カンの自律監査・修正ループ
2. Nori判断が必要な停止条件の明確化
3. `ChatGPT/作業指示案/` の正式な位置付け
4. ChatGPTの常設GitHub書込み許可範囲への同領域追加
5. GitHub書込み前fetch・先行遅延確認の標準化
6. 既存未コミット資産の保全原則
7. ChatGPTとCodexのGitHub同時利用を前提にした分岐対策

まだ正式規程へは反映していない。

## 今後の出力運用

Noriから新たに指示があり、今後は作業指示書案を発行するとき、Markdownファイルと同時に「Codexチャットへそのまま貼れる指示文」も表示する。

## 次に行うこと

新スレッドでは、まずこの規程改訂指示書案を `ChatGPT/作業指示案/` のGitHub運用へ載せ、Nori承認後にCodexへ渡せる状態にする。

その後、コウとカンで改訂案作成・監査・是正・再監査を `適合（承認可）` まで進める。

規程改訂が完了した後、当初の本命である `ChatGPT → GitHub → Data Gateway` 最小レファレンス実装へ戻る。

## 注意

- GitHub上の規程文書はローカルWorkspace正本の参照用ミラーであり、正式規程を置き換えない。
- 正式規程・ROLE・WORKFLOWへの反映はNoriの正式承認後のみ行う。
- GitHub自動監視、Webhook、GitHub Actions、MCP、Codex自動起動は未承認・未導入。
